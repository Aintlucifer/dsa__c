#include <stdio.h>
#include <stdlib.h>


int main() {
    // Seed the random number generator with the current time
    int d;
    
    printf("Random 4-digit number: %d\n", random_number);

    return 0;
}
